@using OrchardCore.Notifications.Workflows.Activities;
@model ActivityViewModel<NotificationTask>

<header>
    <h4><i class="fa fa-envelope"></i>@Model.Activity.GetTitleOrDefault(() => T["Notification"])</h4>
</header>

<span><em>@(Model.Activity.Fields?.Count() ?? 0) @T["properties specified"]</em></span>